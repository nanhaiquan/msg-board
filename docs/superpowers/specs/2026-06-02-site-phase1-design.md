# 集成式网站 第1阶段设计方案

## 概述

将现有留言板改造为集成式导航面板，聚合视频播放 + AI 助手两大模块，保留留言功能。

## 架构

- 单页应用，前端路由切换各模块（导航面板 / 视频 / AI / 留言）
- 后端复用现有 Express + PostgreSQL
- 数据配置存在 `config.json`，用户只需改这个文件

## 第1阶段包含

1. 导航面板（首页仪表盘）
2. 视频板块（搜索+播放B站内容）
3. AI 助手（用户自备 Key，调用 AI API）
4. 保留现有留言板（改名为"社区"）
